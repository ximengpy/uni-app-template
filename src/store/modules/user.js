import store from '@/store'

// 定义存储数据的变量空间
const state = {
  // token: 'token'
}

// mutations
const mutations = {
  /** 设置token */
  // SET_TOKEN: (states, token) => {
  //   states.token = token
  // }
}

// actions
const actions = {
  /** 用户登录 */
  // login({ commit }) {
  //   commit('SET_TOKEN', 'Bearer')
  // }
}

export default {
  namespaced: true,
  state,
  mutations,
  actions
}
